namespace EnityFrameWork2
{
    internal class Program
    {
        static void Main(string[] args)
        {
            #region EF Core ways of mapping
            //1)Convention (default behaviour)
            //2)//Dat Annotations (set of attrubuites)
            //  [ //3)Fluent Apis (set of methods) [DBContext ovveride onmodlcreating)
            //hagat ma3rfsh a3mlha gher blfulent xy pk (composite)
            //
            // Configue model ma3yyesh source code bta3o]
            //4)Configuration classes (config for eac and every model)

            #endregion
        }
    }
}
